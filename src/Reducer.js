export const shoesReducer = (state,action)=>{
    switch(action.type)
    {
        case "add":
            return state
        case "remove":
            return state
        default:
            return state
    }
}